{
  "name": "taste-of-aloha",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "shared/*"
  ],
  "scripts": {
    "dev:web": "npm run dev:web",
    "dev:backend": "npm --workspace apps/backend run dev",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^9.39.1",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "docker": "^1.0.0"
  }
}
